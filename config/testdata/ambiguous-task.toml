[servers.server-1]
host = 'host-1'
user = 'user-1'

  [servers.server-1.tasks.task-1]
    local = [
      { exec = 'echo "from server-1"' },
  ]

[servers.server-2]
host = 'host-2'
user = 'user-2'

  [servers.server-2.tasks.task-1]
    local = [
      { exec = 'echo "from server-2"' },
    ]
