[servers.server-1]
host = 'host'
user = 'user'

  [servers.server-1.tasks.task-1]
    remote = [
      { exec = 'echo task-1' },
    ]

  [servers.server-1.tasks.task-2]
    remote = [
      { exec = 'echo task-2' },
    ]
